# #!/bin/sh
# set -eu

# # Run lint on staged files
# pnpm exec lint-staged

# # Determine default base (prefers master, then main, otherwise previous commit)
# BASE_BRANCH=$(
#   if git rev-parse --verify master >/dev/null 2>&1; then
#     echo master
#   elif git rev-parse --verify main >/dev/null 2>&1; then
#     echo main
#   else
#     echo HEAD~1
#   fi
# )

# # Lint affected projects
# pnpm exec nx affected --target=lint --parallel=3 --skip-nx-cache --base="$BASE_BRANCH" --head=HEAD

# # Run affected tests (only tests for changed code)
# pnpm exec nx affected --target=test --parallel=3 --no-coverage --skip-nx-cache --base="$BASE_BRANCH" --head=HEAD
